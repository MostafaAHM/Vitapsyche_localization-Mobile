import 'package:flutter/material.dart';

class EmargancyCall extends StatelessWidget {
  const EmargancyCall({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Emargancy Call'),
      ),
    );
  }
}
